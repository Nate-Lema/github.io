export function isVowel(vowel:string):boolean{
   if(vowel === "a" || vowel === "e" || vowel === "o" || vowel === "u" || vowel === "i"
   || vowel === "A" || vowel === "E" || vowel === "O" || vowel === "U" || vowel === "I"){
    return true;
   }else{
    return false;
   }
}